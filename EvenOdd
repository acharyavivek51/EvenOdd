#include<stdio.h>
void main()
{
	int num,rem;
	printf("Enter num:");
	scanf("%d",&num);
	rem=num%2;
	if(rem==0)
		printf("Entered number is even");
	else
		printf("Entered number is odd");
}